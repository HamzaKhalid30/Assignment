# A28: Implement a Security Solution (GitHub Project)

## Overview

Implementing a security solution helps in understanding real-world protection mechanisms. It also demonstrates practical cybersecurity skills.

## Practical

1. Open VS Code / IDLE / Terminal
2. Write code:

```python
import socket

target = "127.0.0.1"

print("Scanning target:", target)

for port in range(20, 100):
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.settimeout(0.5)
    
    result = s.connect_ex((target, port))
    
    if result == 0:
        print(f"Port {port} is open")
    
    s.close()
```

3. Run: `python file.py`
</br>
<img width="933" height="354" alt="image" src="https://github.com/user-attachments/assets/4dd57211-7fb5-45f0-9688-1fb9b3b5a2af" />

</br>

## Findings and Details

- The tool scans ports on a target system.
- Identifies open ports and running services.
- Helps detect potential vulnerabilities.

## Reflection

This activity helped me understand how network scanning works. It also showed how simple tools can be used to identify potential security weaknesses.
